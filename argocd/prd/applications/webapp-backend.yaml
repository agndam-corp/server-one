apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: webapp-backend
  namespace: argocd
spec:
  project: default
  source:
    repoURL: git@github.com:agndam-corp/server-one.git
    path: apps/webapp/backend
    targetRevision: HEAD
  destination:
    server: https://kubernetes.default.svc
    namespace: webapp
  syncPolicy:
    automated:
      prune: true
      selfHeal: true