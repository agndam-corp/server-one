apiVersion: traefik.io/v1alpha1
kind: IngressRouteTCP
metadata:
  name: adguard-home-tls
  namespace: adguard-home
spec:
  entryPoints:
    - websecure
  routes:
  - match: HostSNI(`dns-adg.djasko.com`)
    services:
    - name: adguard-home
      namespace: adguard-home
      port: 443
  tls:
    passthrough: true
---
apiVersion: traefik.io/v1alpha1
kind: IngressRouteTCP
metadata:
  name: adguard-dot
  namespace: adguard-home
spec:
  entryPoints:
    - dot
  routes:
  - match: HostSNI(`dns-adg.djasko.com`)
    services:
    - name: adguard-home
      namespace: adguard-home
      port: 853
  tls:
    passthrough: true