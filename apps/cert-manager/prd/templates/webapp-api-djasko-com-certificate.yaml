apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: webapp-api-djasko-com
  namespace: webapp
spec:
  secretName: webapp-api-djasko-com-tls
  issuerRef:
    name: letsencrypt-production-spaceship
    kind: ClusterIssuer
  dnsNames:
  - api.djasko.com